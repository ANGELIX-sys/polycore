# import algorithm
# import seqmath
import sequtils
import arraymancer
import polycore
import random, math
import bitops

# randomize()

# var maze: Tensor[Cell]
# var wall: Cell

# wall.color = [255, 255, 255]
# wall.solid = true

# proc kill(mob: Mob) =

#[
  if
    cellScale == 32: offset = 0.25
    cellScale == 16: offset = 1
    cellScale == 8:  offset = 4
    cellScale == 4:  offset = 16
    cellScale == 2:  offset = 64
    cellScale == 1:  offset = 256


  cellScale ^ (2/x) -> offset 
  ]#